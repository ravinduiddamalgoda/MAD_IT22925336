<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskMangamentApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/theme</item>
        <item name="colorPrimaryVariant">@color/theme</item>
        <item name="android:statusBarColor">@color/theme</item>
    </style>

    <style name="Theme.TaskMangamentApp" parent="Base.Theme.TaskMangamentApp" />
</resources>